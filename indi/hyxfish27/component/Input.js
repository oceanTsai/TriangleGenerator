export class Input {
  constructor (height, size, margin, symbol) {
    this.height = height || '',
    this.size = size || '',
    this.margin = margin || '',
    this.symbol = symbol || ''
  }

  isNumber () {
  }

  isPositive () {

  }

  isNil () {

  }
}
